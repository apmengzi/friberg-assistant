{
  "name": "friberg-license-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute friberg-licenses --local --file schema.sql",
    "db:remote": "wrangler d1 execute friberg-licenses --remote --file schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
