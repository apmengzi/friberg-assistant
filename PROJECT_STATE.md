# Friberg Assistant 项目状态

最后更新：2026-07-31
当前稳定基准：`main` / 0.9.4 Personal
当前候选分支：`feature/live-qol-095` / 0.9.5 Personal
当前候选 PR：#30

## 项目目标

维护“弗一把”CS Major 猜选手辅助插件，包括：

- 游戏官方 646 人题库；
- 严格约束求解器；
- 真实网页 DOM 适配器；
- Edge/Chrome 扩展；
- 本地镜像与自动化回归；
- 个人版与离线授权商业候选版。

## 当前确认完成

- `data/players.game-646.json`：646 条游戏题库；
- `solver.js`：严格求解核心；
- `automation-core.js`：候选/探针与自动化核心；
- `live-dom-adapter.js`：真实页面 DOM 适配层；
- 0.9.4：修复真实反馈首格吞入整行文本，确保年龄、队伍等约束进入求解器；
- GitHub Actions：语法、题库、回归、扩展契约、商业契约、安全扫描和个人版打包；
- CI 只在 `main`、PR 和手动触发运行，避免功能分支每次中间提交产生邮件风暴；
- 旧 `fix/bo3-round-reset` 远程分支已重置为当前 `main`，不再保留自修改工作流。

## 0.9.5 候选功能（PR #30）

- BO3 可见“我的猜测”棋盘元素变化时强制解绑并重扫，避免上一小局历史泄漏；
- 匹配成功后发送一次浏览器/Windows 桌面通知，点击通知聚焦对应标签页；
- `本次匹配自动准备`：默认关闭、仅当前匹配、30 分钟过期、只点击唯一准备按钮一次；
- `首猜 refrezh`：仅当前小局 0/8 时可用，填入并复用现有提交 CD 队列；
- `填入并提交下一猜`：确认新昵称已唯一选中后，复用现有提交/CD队列；
- 新增 0.9.5 合同测试；严格求解器与题库未修改。

## 必须现场验证

1. BO3 第 1/2/3 小局都能在 0/8 使用首猜按钮；
2. `首猜 refrezh` 在网页 CD 中只排队并提交一次；
3. `填入并提交下一猜` 不会提交输入框残留的旧选手；
4. 长时间匹配后只弹一次通知，点击通知可聚焦正确标签页；
5. 一次性自动准备默认关闭，过期后不点击；候选按钮不唯一时不点击；
6. Edge 实测后再验证 Chrome。

## 题库同步策略

- 生产题库以弗一把官方 `shnlfriberg/csgo-major-db` 的 `players.json` 为唯一正式基准；
- Liquipedia 可辅助核查 Coach/Rifler/AWPer 与履历变化，但不得直接把其全部教练分类并入答案池；
- 当前官方仓库仍声明 646 名选手，合法角色为 `Rifler` / `AWPer` / `Coach`；
- 任何题库修改必须在独立 Issue/分支中生成字段差异报告，随后重跑 646×646 全量契约审计并重新计算最佳首猜。

## 受保护文件

除非用户明确批准独立题库任务，否则不得修改：

- `solver.js`
- `extension/solver.js`
- `data/players.game-646.json`
- `extension/data/game-players-646.json`
- 任何用于验证严格规则的 oracle/fixture

若确需修改，必须：

1. 单独 Issue；
2. 单独分支；
3. 说明规则依据与上游数据版本；
4. 运行全量契约审计；
5. 在 PR 中展示改动前后差异。

## 当前优先级

1. 用户真实页面验证 PR #30；
2. 验证通过后合并 0.9.5；
3. 独立执行 Issue #13 上游题库精确差异审计；
4. 在 BO3/通知/一键提交稳定后，再评估 Issue #14 的本次对局完整托管。

## 标准开发循环

1. 用户提供问题、截图或诊断 JSON；
2. 创建或更新一个明确 Issue；
3. 从 `main` 创建独立分支；
4. 修改代码并添加回归测试；
5. 创建 Draft PR；
6. GitHub Actions 通过；
7. 用户在本机 Edge 真实页面验证；
8. 验证结果写入 PR/Issue；
9. 验证通过后再合并到 `main`。
