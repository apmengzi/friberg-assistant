# 真实页面截图状态

这里暂未放置 `extension-installed.png`、`overlay-on-multi.png` 或 `overlay-in-room.png`。

原因不是遗漏：当前 Edge 有两个同标题的真实对局窗口，安全控制无法可靠确认目标 URL，因此没有对浏览器进行安装或页面交互，也不会伪造“已安装 / 已读取真实反馈”的截图。用户只保留一个目标窗口后，才可安全补做真实验收并保存这些截图。
