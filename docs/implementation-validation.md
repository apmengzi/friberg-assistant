# 实现与验证清单

| 验收项 | 代码 / 本地回放 | 真实网页房间 |
| --- | --- | --- |
| 反馈读取调用严格求解器 | 已验证：`recordObservedVisibleFeedback` 接收共享适配器的 7 字段与 3 箭头 | 已确认第一行；第二行 SVG 箭头结构已由真实诊断固化并回放 |
| 多次猜测累积 | 已验证：真实结构夹具从 59 名降至 3 名，手动扫描后仍保持两条历史 | 真实诊断确认第二行存在；修复后待下一局复核 |
| 正确答案保留在候选池 | 已验证：两条真实结构反馈后候选为 3，不会因对方棋盘或箭头缺失降为 0 | 待知道真实答案后现场复核 |
| 悬浮窗显示立即猜 / 最佳探针 | 已验证 | 已看到一次；修复后两行回放仍持续显示 |
| “填入下一猜” | 已实现并在夹具触发原生输入与唯一项选择路径 | 真实下拉选择待验证 |
| “随机首猜并填入” | 扩展与 ScriptCat 已在浏览器夹具验证随机选择、原生输入和唯一项选择 | 真实下拉选择待验证 |
| “提交当前猜测” | 已验证只在用户点击、输入匹配、唯一提交按钮可用时提交，并在等待反馈期间防重复 | 真实站点按钮点击待验证 |
| 两个 Zero 的 playerId 区分 | 已验证：`zero-sk` 不会误选 `zero-cn` | 待真实 DOM 的 playerId 对照 |
| 未知颜色、箭头或行结构 | 已验证：保留历史、暂禁填入、DOM 修复后自动恢复，无“校准页面”依赖 | 真实诊断已触发旧问题；修复后待下一局复核 |
| `/multi`、`/multi/room` 等待对局加载 | 扩展 manifest 与 ScriptCat `@match` 已覆盖；本地等待夹具已验证 | 两个真实路径均已确认 |
| 未压缩扩展根目录含 manifest.json | 已验证：打包脚本与 ZIP 清单检查通过 | 不适用 |
| 诊断导出 | 采集、复制、下载代码和本地 UI 状态已验证 | 已收到真实下载 JSON 并用于本次修复 |

静态审计证据记录在 [static-client-audit.md](static-client-audit.md)。该审计没有把压缩 bundle 推断当作已验证的真实 DOM 合约。

真实房间验证时，请优先提供“采集诊断”生成的脱敏 JSON；如果某个反馈 class 或箭头仍未知，工具会保留此前历史并自动重试，绝不会用未知反馈缩小候选池。
