# 自动化验证报告

验证日期：2026-07-28。

| 验证 | 结果 |
| --- | --- |
| 题库 | 646 名启用选手 |
| 反馈矩阵 | 646 × 646 = 417,316 个严格反馈签名 |
| 核心契约 | `tests/run-automation-core.js` 通过；公开 origin 无法创建自动会话，本地 origin 可在授权合约下行动 |
| 本地模拟 | 1,000 / 1,000 胜利；最多 5 猜，平均 2.764 猜；确定性日志在 [logs/simulation-rounds.jsonl](../logs/simulation-rounds.jsonl) |
| 浏览器本地验收 | 在 `127.0.0.1:4174/mirror/`，人工选择唯一 playerId 后提交成功；新局勾选授权后，全自动针对默认 `z4kr` 在 3 / 8 猜胜利 |
| 浏览器控制台 | 加入内联 favicon 后，本地镜像重载为 0 errors / 0 warnings |
| 扩展静态检查 | `manifest.json` 可解析；内容脚本、后台脚本、worker 和核心均通过 `node --check` |

模拟使用反馈矩阵作真正答案与每次探针之间的严格分区；不是从已知答案直接读值。每一轮的选点按最小最坏分区、再最小期望剩余候选、再最大熵排序，候选不足三人或只余两猜时直接猜合法候选。

未将 Chrome 扩展安装到用户的浏览器配置中，避免在未经确认时改变浏览器状态。打包文件可由用户在开发者模式下显式加载；其公开来源限制已由 `originPolicy` 单元测试覆盖。
