# 真实弗一把网页 DOM 审计状态

审计状态：**已完成真实页面的只读路由审计；对局棋盘/反馈行 DOM 合约仍未完成。** 因此不能把本地镜像结果当作真实站点的反馈读取验收。

2026-07-29 对公开站点进行了只读 DOM 采样：没有对真实页面输入、点击、提交、读取网络、读取 `localStorage`/`IndexedDB` 或读取答案。

| 实际 URL | 只读观察结果 | 结论 |
| --- | --- | --- |
| `https://shnlfriberg.online/multi/` | 页面标题为“多人联机 · 弗一把”；根节点为 React `#root`，主内容包含 `main#main-content.page-scroll` 和大厅 `div.lobby-grid`。当前是创建/匹配/加入房间大厅，页面没有可验证的猜测历史或反馈棋盘。 | 已确认大厅路由真实可访问，但不能据此推断对局表格的选择器。 |
| `https://shnlfriberg.online/multi/room` | 未先真实匹配或加入房间时，主内容只有“正在获取房间状态...”加载状态。未出现玩家棋盘、反馈行、输入框或提交控件。 | 这不能算房间页验收；必须从真实匹配/朋友房流程进入后再审计。 |

此前 Windows 自动化还发现过两个同标题的 Edge 窗口。为了避免误操作别的真实对局，Windows 控制工具要求唯一目标窗口；在无法确认 URL 时已自动停止。

静态产物 [work/game.index.html](../work/game.index.html) 和 [work/game.deployed.js](../work/game.deployed.js) 仍可作为辅助证据：它们表明公开页面使用 React/Vite，且存在 `/multi` 与 `/multi/room` 路由及 realtime 模块；静态产物不能替代当前实时对局 DOM 验收。

## 下一次真实审计的最短步骤

1. 用户只保留一个目标 Edge 窗口，并将其前置到一个已进入房间、能看见自己猜测历史的页面。
2. 只读捕获页面结构，确认自己棋盘和对手棋盘的可见、稳定区分方式。
3. 以人工提交的真实一行验证字段顺序、绿色/黄色/灰色 CSS 信号、数字箭头信号和胜负状态。
4. 将确认后的 DOM 合约写入项目；只有在“自己的棋盘”可被可靠区分时才启用自动读取。
5. 再进行至少三行人工回放验证。任何未知结构都暂停，不采用猜测选择器。

在此之前，ScriptCat 脚本提供用户显式选择棋盘与反馈行、显式颜色/箭头校准、可导出诊断的只读通道；它不能声称已经通过真实页面读取验收。
